<UserControl x:Class="MichaelKoch.UeMR.UI.CollectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MichaelKoch.UeMR.UI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <x:Array x:Key="Teilnehmerliste" Type="{x:Type local:Teilnehmer}">
            <local:Teilnehmer Vorname="Jan" Name="Suchotzki"/>
            <local:Teilnehmer Vorname="Michael" Name="Koch"/>
        </x:Array>
    </UserControl.Resources>
    
    <Grid>
        <DockPanel DataContext="{StaticResource Teilnehmerliste}">
            <ListBox ItemsSource="{Binding}"
                     DisplayMemberPath="Vorname"
                     IsSynchronizedWithCurrentItem="True"/>
            <StackPanel>
                <TextBox Text="{Binding /Vorname}"/>
                <TextBox Text="{Binding /Name}"/>
            </StackPanel>
        </DockPanel>   
    </Grid>
</UserControl>
